package com.nexuserp.notification.domain.model;

/**
 * Canaux de notification supportés par NexusERP.
 * SMS et WHATSAPP sont les canaux dominants sur le marché africain (CI / UEMOA).
 */
public enum NotificationChannel {
    EMAIL, SMS, WHATSAPP, PUSH
}
